const { PrismaClient } = require('@prisma/client'); async function main() { const prisma = new PrismaClient(); try { const patients = await prisma.patient.count(); const appointments = await prisma.appointment.count(); const consultations = await prisma.consultation.count(); const prescriptions = await prisma.prescription.count(); const invoices = await prisma.invoice.count(); const payments = await prisma.payment.count(); const followUps = await prisma.followUp.count(); console.log('Patients:', patients); console.log('Appointments:', appointments); console.log('Consultations:', consultations); console.log('Prescriptions:', prescriptions); console.log('Invoices:', invoices); console.log('Payments:', payments); console.log('Follow-Ups:', followUps); } catch (e) { console.error(e); process.exit(1); } finally { await prisma.(); } } main();
